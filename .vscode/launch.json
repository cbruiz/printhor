{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "run splot",
            "cargo": {
                "args": [
                    "build", "--profile", "dev", "--package", "prinThor", "--features", "s-plot-bin", "--bin", "s_plot"
                ]
            },
            "cwd": "${workspaceFolder}",
            "args": []
        }
    ]
}